# 小卡拼车网页系统

一个小卡拼车网页系统，允许车主创建卡组并管理拼车活动。系统支持多用户实时竞争拼车位置（挤车机制），采用"多带者得"的公平分配规则。

## 特性

- 🎯 **卡组管理**: 车主可以创建多个卡组，添加明星成员
- 🚗 **拼车创建**: 车主和成员都可以创建新拼车
- ⚔️ **竞争机制**: 支持"挤车"功能，多带者得，公平分配
- 📱 **移动端优化**: 专为微信浏览器优化，完美适配手机屏幕
- 🔄 **实时更新**: 使用Server-Sent Events (SSE)实现实时状态同步

## 技术栈

### 前端
- Vue 3 (Composition API)
- TypeScript
- Vite
- Pinia (状态管理)
- Vant (移动端UI组件库)

### 后端
- Node.js
- Express 5
- TypeScript
- SQLite (better-sqlite3)

## 快速开始

### 环境要求
- Node.js 18.0+
- npm 或 pnpm

### 安装依赖

```bash
# 后端
cd backend
npm install

# 前端
cd ../frontend
npm install
```

### 配置环境变量

```bash
# 后端
cp backend/.env.example backend/.env

# 前端
cp frontend/.env.example frontend/.env
```

### 初始化数据库

```bash
cd backend
npm run db:migrate
```

### 启动开发服务器

```bash
# 后端 (终端1)
cd backend
npm run dev

# 前端 (终端2)
cd frontend
npm run dev
```

### 访问应用

- 前端: http://localhost:3000
- 后端API: http://localhost:8000/api

## 项目结构

```
card-carpool/
├── backend/          # 后端服务
│   ├── src/
│   │   ├── models/   # 数据模型
│   │   ├── services/ # 业务逻辑
│   │   ├── api/     # API路由
│   │   └── db/      # 数据库配置
│   └── data/        # SQLite数据库文件
│
└── frontend/        # 前端应用
    ├── src/
    │   ├── components/ # UI组件
    │   ├── pages/      # 页面组件
    │   ├── stores/     # Pinia状态管理
    │   ├── services/    # API客户端
    │   └── styles/     # 样式文件
    └── dist/           # 构建输出
```

## 部署

详细的部署指南请参考 [DEPLOY.md](./DEPLOY.md)

### 快速部署（免费方案）

**推荐方案：Vercel（前端）+ Railway（后端）**

1. **部署后端到Railway**
   - 注册 https://railway.app
   - 导入GitHub仓库
   - 配置环境变量：`PORT=8000`, `CORS_ORIGIN=你的前端域名`
   - 设置构建命令：`npm install && npm run build`
   - 设置启动命令：`npm start`

2. **部署前端到Vercel**
   - 注册 https://vercel.com
   - 导入GitHub仓库，选择 `frontend` 目录
   - 配置环境变量：`VITE_API_BASE_URL=你的后端URL/api`
   - 自动部署完成

3. **更新后端CORS配置**
   - 在Railway中更新 `CORS_ORIGIN` 为前端Vercel域名

详细步骤和故障排查请查看 [DEPLOY.md](./DEPLOY.md)

## 开发

### 代码规范
- ESLint + Prettier
- TypeScript严格模式

### 测试
- 单元测试: Vitest
- E2E测试: Playwright

## 许可证

ISC
